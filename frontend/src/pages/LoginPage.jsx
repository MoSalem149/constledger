/**
 * LoginPage — authentication screen.
 *
 * Sprint 3 will add the full form UI (email + password inputs).
 * For now this is a stub so the route tree doesn't crash.
 */
export default function LoginPage() {
  return (
    <div className="flex flex-col items-center justify-center h-screen bg-bg-main">
      <h1 className="text-3xl font-bold text-text-primary">Login</h1>
      <p className="text-text-secondary mt-2">Authentication form coming in Sprint 3</p>
    </div>
  );
}
