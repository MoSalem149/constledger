/**
 * BudgetVariancePage — planned vs actual budget variance table.
 *
 * Sprint 3 builds the full screen: tabular view showing planned
 * budget vs actual spend per milestone/period for a given contract.
 *
 * Path: /finance/:contractId/variance
 */
export default function BudgetVariancePage() {
  return (
    <div>
      <h1 className="text-2xl font-bold text-text-primary">Budget Variance</h1>
      <p className="text-text-secondary mt-2">Variance table coming in Sprint 3</p>
    </div>
  );
}
