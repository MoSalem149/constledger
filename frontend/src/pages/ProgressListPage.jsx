/**
 * ProgressListPage — actual progress entries timeline for a contract.
 *
 * Sprint 4 builds the full screen: chronological list of progress
 * submissions with status badges (pending, approved, rejected).
 *
 * Path: /finance/:contractId/progress
 */
export default function ProgressListPage() {
  return (
    <div>
      <h1 className="text-2xl font-bold text-text-primary">Progress Entries</h1>
      <p className="text-text-secondary mt-2">Progress timeline coming in Sprint 4</p>
    </div>
  );
}
