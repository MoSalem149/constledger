# API base URL — used by the Axios instance in services/api.js.
# In dev, Vite proxy routes /api/* to the correct backend (see vite.config.js).
# In production, Vercel rewrites do the same.
# Do NOT add trailing slash.
VITE_API_URL=/api
